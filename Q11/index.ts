const names: string[] = ['Rahat', 'Hamza', 'Faiza', 'Malik']; // Store the names of friends in an array called "names"

for (const name of names) { // Loop through each element in the array using a "for...of" loop
  console.log(name); // Print each person's name by accessing each element in the list, one at a time
}
